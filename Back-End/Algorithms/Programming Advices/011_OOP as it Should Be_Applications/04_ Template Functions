
namespace lab
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine(Max(1,2));

        }
        public static T Max<T>(T x, T y)
        {
            return (Comparer<T>.Default.Compare(x, y) > 0) ? x : y;
        }
    }
}
