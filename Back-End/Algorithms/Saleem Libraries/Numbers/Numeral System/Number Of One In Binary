static int numberOfOneInBinary(int number)
    {
        int counter = 0;

        while (number > 0)
        {
            if (number % 2 != 0)
                counter++;

            number = number / 2;
        }

        return counter;
    }
