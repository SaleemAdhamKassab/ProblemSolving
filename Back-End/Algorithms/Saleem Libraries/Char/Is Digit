 static bool isDigit(string s)
        {
            for (int i = 0; i < s.Length; i++)
                if (!((int)s[i] >= 48 && (int)s[i] <= 57))
                    return false;

            return true;
        }
