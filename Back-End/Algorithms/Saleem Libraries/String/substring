static string Substring(string s, int startIndex, int length = 0)
    {
        string result = "";
        int lastIndex = 0;

        if (length == 0)
            lastIndex = s.Length;
        else
            lastIndex = length;

        for (int i = startIndex; i < lastIndex; i++)
            result += s[i];

        return result;
    }
