using System;
using System.Collections.Generic;
using System.Linq;

namespace lab
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine(stringToLowerCase("Write a C# Sharp Program to display the following pattern using the alphabet."));

        }
        static string stringToLowerCase(string s) => s.ToLower();
    }
}
