[HttpGet]
        public IActionResult test()
        {
            Employee employee = new Employee { Id = 1, Name = "saleem" };

            db.Employees.Add(employee);
            db.SaveChanges();

            return Ok(employee);
        }
