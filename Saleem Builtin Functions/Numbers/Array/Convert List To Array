static int[] convertListToArray(List<int> lst)
        {
            int[] result = new int[lst.Count];

            for (int i = 0; i < lst.Count; i++)
                result[i] = lst[i];

            return result;
        }
