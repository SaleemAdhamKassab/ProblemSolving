static bool isUpperChar(char c) => (int)c >= 65 && (int)c <= 90;
static int countCapitalLetters(string s)
        {
            int counter = 0;

            for (int i = 0; i < s.Length; i++)
                if (isUpperChar(s[i]))
                    counter++;

            return counter;
        }
