static int[] fillArrayWithDefaultValue(int[] arr, int defaultValue)
        {
            for (int i = 0; i < arr.Length; i++)
                arr[i] = defaultValue;

            return arr;
        }
