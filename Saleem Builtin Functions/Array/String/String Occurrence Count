static int occurrenceCount(string [] arr, string s)
        {
            int counter = 0;
            for (int i = 0; i < arr.Length; i++)
                if (arr[i] == s)
                    counter++;

            return counter;
        }
