DECLARE @TableName NVARCHAR(256) = 'YourTableName';
DECLARE @SQL NVARCHAR(MAX) = '';

SELECT @SQL = STRING_AGG('INSERT INTO ' + @TableName + ' (' + STRING_AGG(QUOTENAME(COLUMN_NAME), ', ') + ') VALUES (' + STRING_AGG('''' + CAST(VALUES AS NVARCHAR(MAX)) + '''', ', ') + ');', CHAR(10))
FROM (
    SELECT COLUMN_NAME, 
           (SELECT 
                STRING_AGG(CAST(COLUMN_NAME AS NVARCHAR(MAX)), ', ') 
            FROM YourTableName) AS VALUES
    FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_NAME = @TableName
) AS T;

PRINT @SQL;
