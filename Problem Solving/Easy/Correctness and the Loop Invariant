function insertionSort(A: number[]) {
    var j = 0;
    for (var i = 0; i < A.length; i++) {
        var value = A[i];
        j = i - 1;
        while (j >= 0 && value < A[j]) {
            A[j + 1] = A[j];
            j = j - 1;
        }
        A[j + 1] = value;
    }
    console.log(' ', A)
}
