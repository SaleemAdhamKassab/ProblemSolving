static char[] stringToArray(string s)
        {
            char[] result = new char[length(s)];

            for (int i = 0; i < s.Length; i++)
                result[i] = s[i];

            return result;
        }
 
