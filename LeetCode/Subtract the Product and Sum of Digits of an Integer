 static int SubtractProductAndSum(int n)
        {
            int sum = 0, prod = 1;
            while (n > 0)
            {
                if (n > 0) prod *= n % 10;
                sum += n % 10;
                n = n / 10;
            }
            return prod - sum;
        }
