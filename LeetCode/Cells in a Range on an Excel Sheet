static IList<string> CellsInRange(string s)
        {
            List<string> result = new List<string>();

            for (char i = s[0]; i <= s[3]; i++)
                for (int j = int.Parse(s[1].ToString()); j <= int.Parse(s[4].ToString()); j++)
                    result.Add(i + (j).ToString());

            return result;
        }
