public class Solution {
    public string GenerateTheString(int n) {
         if (n == 1) return "a";

            string result = "";

            for (int i = 1; i < n; i++)
                result += 'a';

            return n % 2 != 0?result += 'a': result += 'b';
    }
}
