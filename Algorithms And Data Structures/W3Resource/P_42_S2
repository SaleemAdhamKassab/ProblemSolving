namespace lab
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(p42("w3r"));
        }
        static string p42(string s) =>
            s.Length < 4 ? s.ToUpper() :
            s.Substring(0, 4).ToLower() + s.Substring(4);
    }
}
