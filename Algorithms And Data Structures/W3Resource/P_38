using System;

class URI
{
    static void Main(string[] args)
    {
        Console.WriteLine(p38("PHP Tuotorial"));
    }
    static bool startWithPH(string input) => input[0] == 'P' && input[1] == 'H' ? true : false;
    static string p38(string input) => startWithPH(input) ? "PH" : "";
}
