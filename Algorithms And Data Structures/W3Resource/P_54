using Lab.Bank.Views.ManageUsers;

namespace lab
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(p54(1900));
        }
        static int p54(int year) => (int)(year / 100) + ((year % 100 == 0) ? 0 : 1);
    }
}
