test
public static int countOfNumber (List<int> lst, int num)
        {
            int counter = 0;

            for (int i = 0; i < lst.Count; i++)
                if (lst[i] == num)
                    counter++;

            return counter;
        }
