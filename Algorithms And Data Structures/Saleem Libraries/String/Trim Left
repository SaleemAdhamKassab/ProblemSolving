static string TrimLeft(string s)
        {
            string result = "";
            bool stop = false;

            for (int i = 0; i < s.Length; i++)
            {
                if (s[i] == ' ' && stop == false)
                    continue;
                else
                    stop = true;

                result += s[i];
            }

            return result;
        }
