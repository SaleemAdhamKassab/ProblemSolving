static string removeCharacterFromString(string s, char charToRemove)
        {
            string result = "";

            for (int i = 0; i < s.Length; i++)
                if (s[i] != charToRemove)
                    result += s[i];
           
            return result;
        }
