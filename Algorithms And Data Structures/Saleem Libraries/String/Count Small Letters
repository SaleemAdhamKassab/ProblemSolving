static bool isLowerChar(char c) => (int)c >= 97 && (int)c <= 122;
static int countSmallLetters(string s)
        {
            int counter = 0;

            for (int i = 0; i < s.Length; i++)
                if (isLowerChar(s[i]))
                    counter++;

            return counter;
        }
