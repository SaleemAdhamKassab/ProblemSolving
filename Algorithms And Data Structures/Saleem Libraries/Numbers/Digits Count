static int digitsCount(int number)
        {
            int numberDigitsCount = 0;

            while (number > 0)
            {
                numberDigitsCount++;
                number = number / 10;
            }

            return numberDigitsCount;
        }
