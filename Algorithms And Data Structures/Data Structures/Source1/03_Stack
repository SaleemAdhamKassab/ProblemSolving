using Lab.DS;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace lab
{
    class Program
    {
        static void Main()
        {
            SaleemStack saleemStack = new SaleemStack();
            saleemStack.push(10);
            saleemStack.push(5);
            Console.WriteLine("top is: {0}", saleemStack.top());
            saleemStack.pop();
            Console.WriteLine("top is: {0}", saleemStack.top());
        }
    }
}
