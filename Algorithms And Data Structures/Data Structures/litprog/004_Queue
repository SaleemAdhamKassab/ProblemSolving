using Lab.DS;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace lab
{
    class Program
    {
        static void Main()
        {
            SaleemQueue saleemQueue = new SaleemQueue();
            saleemQueue.enqueue(5);
            saleemQueue.enqueue(10);
            saleemQueue.enqueue(20);
            Console.WriteLine(saleemQueue.front());
            saleemQueue.dequeue();
            Console.WriteLine(saleemQueue.front());
        }
    }
}
