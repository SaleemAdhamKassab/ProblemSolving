 [HttpGet]
        public IActionResult test()
        {
            List<Product> products = db.Products.Where(e => e.Price == 18).ToList();
                                 

            return Ok(products);
        }
