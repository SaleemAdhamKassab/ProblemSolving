https://www.w3schools.com/sql/trysql.asp?filename=trysql_op_less_than2 
 
 [HttpGet]
        public IActionResult test()
        {
            List<Product> products = db.Products.Where(e => e.Price <= 30).ToList();

            return Ok(products);
        }
