https://www.w3schools.com/sql/trysql.asp?filename=trysql_op_greater_than2


        [HttpGet]
        public IActionResult test()
        {
            List<Product> products = (from obj in db.Products
                                      where obj.Price >= 30
                                      select obj).ToList();

            return Ok(products);
        }
