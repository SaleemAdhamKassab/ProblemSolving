 static string reverseBasedOnFirstAndLastIndex(string s, int firstIndex, int lastIndex)
    {
        string result = "";
        for (int i = firstIndex; i <= lastIndex; i++)
            result += s[i];

        return result;
    }
